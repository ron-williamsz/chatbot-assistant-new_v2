# ====================================
# CONFIGURAÇÕES DE SEGURANÇA SQLITE
# ====================================

# Habilitar configurações avançadas de segurança SQLite
# true = ativa PRAGMAs de segurança, WAL mode, etc.
# false = usa configurações básicas
SQLITE_SECURE=true

# Habilitar sistema de backup automático
# true = permite criação de backups via interface admin
# false = desabilita funcionalidade de backup
DB_BACKUP_ENABLED=true

# Chave de criptografia para SQLite (64 caracteres hexadecimais)
# Gere uma nova chave com: openssl rand -hex 32
# IMPORTANTE: Mantenha esta chave segura e não compartilhe
SQLITE_ENCRYPTION_KEY=sua_chave_de_criptografia_sqlite_64_caracteres_aqui

# ====================================
# COMO USAR:
# ====================================
# 1. Adicione estas variáveis ao seu arquivo .env
# 2. Substitua 'sua_chave_de_criptografia_sqlite_64_caracteres_aqui' por uma chave real
# 3. Configure as outras variáveis conforme necessário
# 4. Reinicie os containers Docker para aplicar as mudanças

# ====================================
# GERAR CHAVE DE CRIPTOGRAFIA:
# ====================================
# Execute este comando para gerar uma chave segura:
# openssl rand -hex 32

# ====================================
# EXEMPLO DE CONFIGURAÇÃO COMPLETA:
# ====================================
# SQLITE_SECURE=true
# DB_BACKUP_ENABLED=true
# SQLITE_ENCRYPTION_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456 